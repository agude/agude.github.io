[project]
name = "agude-blog-scripts"
version = "0.0.0"
requires-python = ">=3.12"

[dependency-groups]
dev = ["pytest", "pyyaml", "requests"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["content", "diagnostics", "metadata", "ranking", "../.claude/skills/stub-book/scripts"]
